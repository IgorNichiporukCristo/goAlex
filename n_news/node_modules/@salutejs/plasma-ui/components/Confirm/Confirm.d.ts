import { ReactNode } from 'react';
import { ButtonProps } from '../Button';
export declare type Direction = 'horizontal' | 'vertical' | 'horizontal-reverse' | 'vertical-reverse';
export interface ConfirmProps {
    /**
     * Заголовок подтверждения
     */
    title: string;
    /**
     * Подзаголовок подтверждения
     */
    subtitle?: string;
    /**
     * Текст кнопки согласия
     */
    approveText: string;
    /**
     * Текст кнопки отказа
     */
    dismissText?: string;
    /**
     * Очередность кнопок. Вместо этого свойства лучше использовать `buttonsDirection`
     * @deprecated
     */
    reverseButtons?: boolean;
    /**
     * Вид подтверждения
     */
    view?: Exclude<ButtonProps['view'], 'checked' | 'clear' | 'overlay'>;
    /**
     * Состояние подтверждения
     */
    visible: boolean;
    /**
     * Обработчик согласия
     */
    onApprove: () => void;
    /**
     * Обработчик отказа
     */
    onDismiss?: () => void;
    /**
     * Компонент снизу
     */
    extraContent?: ReactNode;
    /**
     * Направление для группы кнопок
     */
    buttonsDirection?: Direction;
}
/**
 * Сообщение подтверждения действия пользователя.
 */
export declare const Confirm: (props: ConfirmProps) => JSX.Element;
